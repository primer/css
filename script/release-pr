#!/bin/bash
set -e
export PATH="$PATH:$(npm bin)"

echo "Publishing PR (canary) release..."
commit=$(git rev-parse --short HEAD)
lerna publish patch --preid="pr.$commit" --npm-tag=pr $@

echo "Updating the changelog..."
lerna-changelog
