#!/bin/bash
set -e
export PATH="$PATH:$(npm bin)"

echo "Publishing release candidate..."
commit=$(git rev-parse --short HEAD)
lerna publish prerelease --exact --preid="rc.$commit" --npm-tag=rc $@

echo "Updating the changelog..."
lerna-changelog
