{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "updated": "lerna updated",
    "diff": "lerna diff",
    "publish": "lerna publish --skip-git",
    "publish-rc": "npm run publish -- --npm-tag=rc",
    "publish-canary": "npm run publish -- --canary=commit --cd-version=patch"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
