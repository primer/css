language: node_js
node_js:
  - 7

before_script:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_API_KEY}" > .npmrc
  - npm whoami
  - echo lerna bootstrap

script:
  - echo npm test

after_success:
  - echo script/cibuild --yes
